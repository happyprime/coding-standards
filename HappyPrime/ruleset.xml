<?xml version="1.0"?>
<ruleset name="HappyPrime">
	<description>Happy Prime PHP coding standards.</description>

	<!-- Check all PHP files in directory tree by default. -->
	<arg name="extensions" value="php"/>
	<file>.</file>

	<!-- Show progress and sniff codes in all reports -->
	<arg value="ps"/>

	<!-- Rules: Check PHP version compatibility -->
	<config name="testVersion" value="7.4-"/>

	<!-- Ignore 3rd party libraries loaded by NPM -->
	<exclude-pattern>*/node_modules/*</exclude-pattern>

	<!-- Ignore 3rd party libraries loaded by Composer -->
	<exclude-pattern>*/vendor/*</exclude-pattern>

	<!-- Ignore asset files generated by @wordpress/scripts -->
	<exclude-pattern>*.asset.php</exclude-pattern>
	<exclude-pattern>*.scss.php</exclude-pattern>
	<exclude-pattern>*.css.php</exclude-pattern>

	<rule ref="PHPCompatibilityWP"/>
	<rule ref="VariableAnalysis" />
	<rule ref="WordPress-Extra" />
	<rule ref="WordPress-Docs" />

	<!-- WordPress should allow short array syntax. -->
	<rule ref="Universal.Arrays.DisallowShortArraySyntax">
		<exclude name="Universal.Arrays.DisallowShortArraySyntax.Found"/>
	</rule>
</ruleset>
